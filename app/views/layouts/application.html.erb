<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <%= csrf_meta_tags %>
    <title><%= content_for?(:title) ? yield(:title) : t('home.title') %></title>

    <% if content_for?(:tags) %>
      <%= yield(:tags) %>
    <% else %>
      <meta name="description" content="<%= t('home.description') %>"/>
      <meta name="og:description" content="<%= t('home.description') %>"/>
      <meta name="og:image" content="<%= image_url 'icon.png' %>"/>
    <% end %>
    <meta name="og:site_name" content="<%= t('site-name') %>"/>
    <meta name="fb:app_id" content="<%= ENV['FB_PAGE_ID'] %>" />

    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <%= render 'shared/header' %>
    <%= yield %>

    <footer>
      <div class="container">
        <div class="website">Todo Calderas</div>
        <div class="author">
          Miguel Ángel Cedrón - Técnico Superior en Mantenimiento Industrial
        </div>
      </div>
    </footer>

    <script type="text/javascript" data-turbolinks-eval=false>
       WebFontConfig = {
         google: {
           families: ['Open Sans:400,300', 'Titillium Web:400,700']
         }
       };

       (function(d) {
          var wf = d.createElement('script'), s = d.scripts[0];
          wf.src = 'https://ajax.googleapis.com/ajax/libs/webfont/1.6.26/webfont.js';
          wf.async = true;
          s.parentNode.insertBefore(wf, s);
       })(document);
    </script>

    <script>
      (function(i,s,o,g,r,a,m){i['GoogleAnalyticsObject']=r;i[r]=i[r]||function(){
      (i[r].q=i[r].q||[]).push(arguments)},i[r].l=1*new Date();a=s.createElement(o),
      m=s.getElementsByTagName(o)[0];a.async=1;a.src=g;m.parentNode.insertBefore(a,m)
      })(window,document,'script','//www.google-analytics.com/analytics.js','ga');

      ga('create', 'UA-46037788-1', 'todocalderas.com.ar');
      ga('send', 'pageview');

    </script>
  </body>
</html>
